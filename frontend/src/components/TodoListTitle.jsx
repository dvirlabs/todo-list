import { React } from "react";

const TodoListTitle = () => {
    return (
        <div className="todoListTitle">
            <h1>Todo List</h1>
        </div>
    );
};
export default TodoListTitle;